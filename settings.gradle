// AE2 VM 1.16.1 fork — Forge 36.0.x + ForgeGradle 4.1.x + Gradle 5.6.4 + Java 8 daemon
//
// 1.16.1 fork 的 settings.gradle 比 1.17.1 fork 简单很多：
//   FG4 时代没有 pluginManagement {} / dependencyResolutionManagement {} 这些块
//   只有 rootProject.name 一行就够（FG4 用 buildscript classpath，不走 plugins DSL）

rootProject.name = 'ae2-vm-addon-1.16.1-forge'