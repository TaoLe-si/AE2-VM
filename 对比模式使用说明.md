# AE2-VM 原版对比模式（VM vs Vanilla Plan）

## 用途

同一笔订单，**同时打印 VM 计算出的 plan 和原版 AE2 计算出的 plan**，格式完全一致，
直接 diff 就知道数据计算是否有问题（pattern 数量、每个 pattern 的合成次数、used/missing）。

## 开启方式（任选其一）

| 方式 | 操作 |
|---|---|
| A. 标记文件（最简单） | 在游戏实例的 `config` 文件夹创建一个**空文件** `ae2vm.comparevanilla` |
| B. JVM 参数 | 启动参数加 `-Dae2vm.compareVanilla=true` |
| C. Cloth Config | 已装 Cloth Config 时，把 `config/ae2vm.json` 的 `compareVanilla` 改为 `true` |

**同时可选（强烈建议对比时开启）**：仅保留 VM 这一个 mixin，去掉其他 mixin 的干扰：

| 方式 | 操作 |
|---|---|
| A. 标记文件 | 在 `config` 文件夹创建空文件 `ae2vm.onlyvm` |
| B. JVM 参数 | `-Dae2vm.onlyVmMixins=true` |

## 日志输出

每次 VM 计算成功后，日志出现两行：

```
[AE2-VM-CMP] VM: patterns[gtceu:x x12(*1=12), ...] used[ ...=... ] missing[ ...=... ] emitted[...] bytes=...
[AE2-VM-CMP] VANILLA: patterns[gtceu:x x12(*1=12), ...] used[ ...=... ] missing[ ...=... ] emitted[...] bytes=...
```

- `patterns[` 内：每个 pattern = `输出物品 x合成次数(*每craft输出=总产量)`
- `used/missing/emitted[`：与 VM 的 Plan DIAG 相同的键值格式
- 两份数据的 `patterns` 应逐项一致；`used` 应一致；差异即为计算问题

## 注意事项

1. **原版计算可能很慢**（GTL mega-chain 可达 10-30s）。有 90 秒超时保护，
   超时只打 warn、不影响 VM 结果。
2. compareVanilla 只在 **VM 计算成功**后触发；VM 失败回退原版时不会重复对比。
3. onlyVmMixins 会同时禁用 EXTRACT4 日志、pattern buffer 同步等 mixin——
   **只用于对比测试**，测完删除 `config/ae2vm.onlyvm` 恢复。
4. 对比完成后删除 `config/ae2vm.comparevanilla`（或还原配置），避免每次下单都多跑一次原版。

## 推荐流程

1. 先开 `ae2vm.comparevanilla` + `ae2vm.onlyvm`，重启游戏。
2. 取消旧的卡住任务（echoite/antimatter），重新下单。
3. 抓 `[AE2-VM-CMP]` 两行 + `[AE2-VM DIAG] Plan demand`。
4. 把两份数据发给我，或自行 diff —— 不一致即为计算 bug，我来修。
