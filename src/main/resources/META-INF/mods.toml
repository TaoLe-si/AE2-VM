modLoader="javafml"
loaderVersion="[47,)"
license="LGPL-3.0"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
logoFile="ae2vm.png"
authors="Tao  QQ 2584300846"
description='''
Replaces AE2 recursive crafting calculation with a stack-based virtual machine for massive performance gains.

Architecture:
- Patterns are compiled to bytecode at encode time
- BigInteger stack for unlimited precision
- JIT bundle caching eliminates repeated subtree calculations
- Binary decomposition for multiplier recipes (cts>1)
- Memoization for repeated single-craft calls (cts=1)
- 2,400x faster for deep crafting trees (90s -> 38ms)
- Compatible with ECO crafting CPUs
'''

[[mixins]]
config="${mod_id}.mixins.json"

[[dependencies.${mod_id}]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="[${minecraft_version}]"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="ae2"
    mandatory=true
    versionRange="${ae2_version_range}"
    ordering="AFTER"
    side="BOTH"

# Cloth Config API（可选）：装了才有 config/ae2vm.json（proxy.enabled 开关）；不装用默认值
[[dependencies.${mod_id}]]
    modId="cloth_config"
    mandatory=false
    versionRange="[11.1,12)"
    ordering="NONE"
    side="BOTH"
