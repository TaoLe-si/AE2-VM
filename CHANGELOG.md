# Changelog / 更新日志

版本号基于 `1.9.0`：每次编译 `mod_version` +0.0.1（1.9.0 → 1.9.1 → …）。

## [1.9.11] - 2026-08-07

### 修复
- **JIT 命中率回落**（与 1.21.1 一致）：v1.9.10 级联丢弃导致空库存下每次全量重捕获。改为不丢弃 bundle（结构完整）+ `applyBundleDirect` 对 `missing` 实时验证（有货取货、无货报缺）。JIT 命中率 100%，正确性保持。

## [1.9.10] - 2026-08-07

### 修复
- **缓存卫生级联丢弃**（与 1.21.1 一致）：v1.9.8 的 `subBundlesComplete` 只查直接子，24 层深 Fibonacci 链中高层复用导致孙级低层可合成项永不重新捕获 → 第二次下单报缺失。修复：缓存卫生级联丢弃（丢弃一个 bundle 时，itemNeeds 依赖它的父也丢弃），保证缓存子树完整。

## [1.9.8] - 2026-08-07

### 修复
- **缓存配方丢失导致合成计划变小**（与 1.21.1 一致）：空库存下低层配方 bundle 因 missing 非空被缓存卫生丢弃，高层 bundle 复用导致低层子链永不重新捕获 → 整个子链变 missing、计划变小。修复：复用缓存 bundle 前检查 `subBundlesComplete`（itemNeeds 子 bundle 齐全），缺失则重新捕获拉起子链。

## [1.9.7] - 2026-08-07

### 修复
- **stock-aware 子项双重记账导致合成数量错误**（真正根因，与 1.21.1 一致）：中间产物既有配方又有库存且 `0 < 库存 < 需求` 时，父 bundle 与 stock-aware 分支重复记账网络库存 → 误报 missing。聚合期用 `subtractStockFromNetwork` 从父 bundle `used` 需求扣除网络库存部分，父只提取合成缺口。
- **清理调试日志**：仅保留性能日志（微秒）。

## [1.9.5] - 2026-08-07

### 修复
- **跨请求库存快照污染导致合成数量错误**：VM 复用后 `realStockCache` 未在 `execute()` 重置，第二次起的请求读到旧库存 → 数量错误。现在每次请求重新快照真实库存（与 1.21.1 一致）。
- **日志微秒精度**：`calc time` 与 `VM OK` 改为微秒（`X us (X.XX ms)`），不再出现 `0ms`。

## [1.9.2] - 2026-08-07

### 优化
- **跨请求 JIT 缓存复用**：`AE2VMCrafting` 按网络（grid）缓存并复用 `CraftingVM` 实例，`bundleCache` 跨请求存活（第二次起的请求 JIT 命中率接近 100%）。`CraftingVM` 支持 `setPatternResolver`、`execute()` 同步、缺料 bundle 缓存卫生。与 1.21.1 一致。
- **日志**：保留性能计算日志（`[AE2-VM] calc time: X ms`）。

## [1.9.0] - 2026-08-07

### 变更
- **版本统一到 1.9.0**：与 1.21.1 双版本统一基线。版本号自增逻辑不变（每次编译 PATCH+1）。

## [1.8.23] - 2026-08-07

### 修复
- **自生长环裁切**：未播种的 `A → 2A` 纯自环样板（主输出键 == 每个输入槽的键）永远不再 firing，需求只从库存取、短缺报 missing。修复凭空复制物品（与 1.21.1 的 1.8.26 一致）。
- 此前 1.8.22 已修复「库存优先取用最后一份不发送物品」（stock-aware sub-craft 记录真实网络库存到 usedItems）。

## [1.8.22] - 2026-08-07

### 修复
- **下单空白样板/含流体配方时，最后一份不发送物品**：
  - 场景：批量合成（如空白样板），当某个中间产物/流体**网络里有库存、可优先取用**（stock-aware sub-craft 逻辑）时，计划只声明了库存用量却把 `usedItems` 记成 0。CPU 提交计划时从网络取走该批库存后，**最后一份**因缺中间产物/流体取料失败 → 组装机收不到物品。
  - 根因：root 捕获阶段父配方的 claim-EXTRACT 已把该库存从沙箱 sim 消耗掉，但 `extractIsClaim=true` 未写入 `usedItems`，`revertBundle` 也不会恢复 → sim 被排空，后续 `simulation.extract()` 返回 0，`usedItems` 加成了 0。
  - 修复：把**真实网络库存 `fromStock`** 直接写入 `usedItems`（不再信任被排空的 sim 返回值），保证 CPU 在提交时能取到该批库存。该计算逻辑与 1.21.1（1.8.25）保持一致。
